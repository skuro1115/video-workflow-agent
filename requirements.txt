# Runtime deps.
#
# The core pipeline (probe → detect → plan → export) shells out to
# `ffmpeg` / `ffprobe` for the heavy lifting. The Python deps below are:
#
#   - PyYAML: required for the inbox workflow (config.yaml + *.task.yaml).
#     Stdlib has no YAML reader, and YAML beats JSON for human-edited
#     config (comments + less punctuation noise).
#   - yt-dlp / chat-downloader: only for the optional URL ingest step
#     (scripts/fetch.py). The pipeline itself runs without them.

PyYAML>=6.0                # config.yaml + inbox/*.task.yaml
yt-dlp>=2024.12.0          # YouTube / Twitch video + YouTube live-chat replay
chat-downloader>=0.2.8     # Twitch VOD chat (yt-dlp does not extract it)

# scenedetect>=0.6         # shot/scene-change based hotspot detection (planned)
# faster-whisper>=1.0      # transcript-based hotspot scoring (planned)
# openai>=1.40             # LLM-rated highlight selection (planned)
# moviepy>=1.0             # in-process editing if shelling out is not enough
# pydantic>=2.0            # if config grows beyond dataclasses
